export type Metadata = {
    version: string
    last_updated: string
    source: string
    total_provinces: number
    total_districts: number
    total_subdistricts: number
    total_postal_codes: number
}
